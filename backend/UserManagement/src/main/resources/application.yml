spring:
  application:
    name: UserManagement
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    password: PhuongTay110903@
    url: jdbc:mysql://localhost:3306/spring_user_management?useSSL=false&useUnicode=true&characterEncoding=UTF-8
    username: root
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
    show-sql: true
